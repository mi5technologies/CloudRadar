rules:
  - id: lambda_env_secrets
    resource_type: lambda
    category: IDENTITY
    severity: high
    title: Lambda function may have secrets in environment variables
    description: Environment variable names or values suggest hardcoded credentials
    condition: {op: true, key: env_secrets_detected}
    required: false
